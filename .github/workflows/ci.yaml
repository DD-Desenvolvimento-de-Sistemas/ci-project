name: ci-project-sonar

on:
  pull_request:
    branches:
      - main
      - develop

jobs:
  run-ci:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [16.x]
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@master
      - run: yarn test